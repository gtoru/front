export declare class Response<TData> {
    responseCode: number;
    responseData: TData;
    errorInfo: ErrorInfo;
    constructor(responseCode: number, responseData: TData, errorInfo?: ErrorInfo);
}
export declare type ErrorInfo = string;
